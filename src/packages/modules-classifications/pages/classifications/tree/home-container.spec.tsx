import { describe, it } from "vitest";

// Le fichier source home-container.tsx n'existe pas.
// Ces tests sont mis en attente jusqu'à la création du composant.
describe.skip("home-container", () => {
  it("placeholder", () => {});
});
