import React from 'react';
import { withRouter } from 'react-router-dom';
import { Menu } from '@inseefr/ui';

const MenuRoles = () => {
	return <Menu paths={[]} />;
};

export default withRouter(MenuRoles);
